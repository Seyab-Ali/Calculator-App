import 'package:flutter/material.dart';

const Color blackColor = Colors.black;
const Color greenColor = Colors.green;

const headingTextStyle =
    TextStyle(fontSize: 35, color: greenColor, fontWeight: FontWeight.bold);
